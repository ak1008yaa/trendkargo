# حافظهٔ پروژه — Trend Cargo

## معرفی و نحوه استفاده
- هدف: شروع سریع جلسات بعدی بدون بازخوانی کل مخزن و کاهش مصرف توکن.
- تاریخ تهیه: 2026-09-17 (نسخه ۲)؛ مبنای بررسی: شاخه `main` بعد از ادغام یکپارچه، کامیت مرج `6b6aa98`.
- ریشه پروژه: `E:\New folder (2)` — تنها پوشه کاری پروژه؛ پوشه قدیمی `E:\New folder (2).worktrees` حذف شده است.
- ابتدا این فایل و راهنمای `AGENTS.md` را بخوانید؛ سپس فقط فایل‌های مرتبط با درخواست جدید را بررسی کنید.
- این سند خلاصهٔ کد و تاریخچهٔ بررسی‌شده است، نه آرشیو تمام گفتگوهای گذشته یا تضمین سلامت همه امکانات. در تعارض، کد فعلی مرجع است.

## امکانات و معماری
سایت فارسی RTL برای کارگو و خرید مستقیم از تمو، شین و آمازون؛ HTML/CSS/JavaScript خالص، بدون فریمورک یا مرحله build. قابل میزبانی استاتیک و Vercel است.

امکانات موجود در ساختار پروژه: کاتالوگ محصولات، مودال محصول، محاسبه قیمت و نرخ ارز، سفارش سفارشی و ارتباط واتساپ، تم روشن/تیره، پنل مدیریت محصولات و تخفیف‌ها و تنظیمات، ایجاد و چاپ فاکتور، بلاگ و مدیریت مقالات، PWA و سرویس‌ورکر.

state مرورگر در localStorage نگهداری می‌شود و لایه پشتیبان آن سرور است. بک‌اند Express (`backend/`) سرویس اصلی سایت داینامیک است: هم سایت و هم API را روی یک پورت سرو می‌کند و پایه به‌روزرسانی لحظه‌ای ادمین↔سایت است.

## ساختار و محل مراجعه
| مسیر | مسئولیت |
| --- | --- |
| `index.html` | صفحه اصلی، فروشگاه و ساختار DOM |
| `js/script.js` | کاتالوگ، نرخ ارز، محاسبه‌گر، مودال، تم و ذخیره‌سازی |
| `css/style.css` | استایل‌ها، RTL و تم‌ها |
| `admin.html` | مدیریت محصولات، فاکتور، تخفیف و تنظیمات |
| `invoice.html` | پیش‌نمایش قابل چاپ فاکتور |
| `blog.html`، `js/blog.js` | نمایش بلاگ |
| `js/blog-data.js`، `js/blog-admin.js` | داده اولیه و مدیریت بلاگ |
| `assets/img/` | تصاویر محلی محصولات و بلاگ؛ `placeholder.svg` و `CREDITS.md` |
| `data/products.seed.json` | نسخه مرجع کاتالوگ؛ بارگذاری خودکار ندارد |
| `backend/server.js`، `backend/routes/` | API اختیاری Express |
| `backend/data/` | فایل‌های JSON محصولات، نرخ‌ها، تخفیف‌ها، اخبار، پیشنهاد و نظرات |
| `sw.js`، `manifest.json` | کش و PWA |
| `vercel.json` | هدرهای امنیتی، CSP، کش و cleanUrls |
| `robots.txt`، `sitemap.xml` | تنظیمات خزیدن و نقشه سایت |
| `tools/localize-images.cjs` | محلی‌سازی تصاویر |
| `tools/smoke-test.cjs` | ابزار smoke موجود؛ اجرای آن در این جلسه بررسی نشده |
| `pack_project.py` | بسته‌بندی استاتیک |
| `README.md`، `AGENTS.md` | راهنمای اجرا و قراردادهای توسعه |

## کارهای اخیر و تاریخچه
### تاریخچه یکپارچه (پس از مرج 6b6aa98)
مخزن تک‌شاخه‌ای است؛ همه کامیت‌های زیر اکنون در `main` هستند:
- `dc4c30e`: محلی‌سازی تصاویر، تم روشن با سبک iOS، اصلاح جهت فلش نظرات در RTL، دکمه‌های اشتراک‌گذاری و دو گزارش خبری فناوری. README، راهنمای تصاویر و placeholder نیز به‌روز شده‌اند.
- `a61fe78`: به‌روزرسانی مدیریت، استایل‌ها، اسکریپت‌ها و بلاگ؛ اضافه‌شدن فایل‌های داده، نمایش و مدیریت بلاگ.
- `ded8706` و `f15ca39`: اصلاح دامنه نقشه سایت و حذف لینک‌های anchor.
- `6b6aa98` (مرج): اتصال تاریخچه main (Supabase، SEO/GEO `3ee2d50`، aurora `ca2c433`) به شاخه کاری؛ درخت نهایی = درخت `agents/12002`.
- فقط پوشه `tests/` و فایل‌های Supabase/API و `llms.txt` مربوط به آزمایش‌های لغوشده ورکسل‌اند؛ در برنچ پشتیبان `backup/pre-unify-20260917` نگهداری می‌شوند، نه در main.

## قراردادهای مهم
- تغییرات کوچک و هدفمند؛ حفظ فارسی، RTL، شناسه‌های DOM، جریان واتساپ و نصب PWA؛ بدون افزودن فریمورک.
- اسکیمای محصول: `id, category, catName, title, price, rawPrice, tag, mainImg, gallery, desc, specs`.
- کلیدها و شکل داده‌های localStorage را حفظ کنید. مرجع مرکزی `STORAGE_KEYS` در ابتدای `js/script.js` است؛ محصولات: `trendcargo_custom_products` و پیشنهاد ویژه: `trendcargo_special_offer`. این فهرست کامل نیست؛ پیش از تغییر ذخیره‌سازی، مصرف‌کننده‌های مدیریت، فاکتور و بلاگ را نیز بررسی کنید.
- تصاویر محتوا محلی باشند؛ تصویر خارجی را با ابزار محلی‌سازی منتقل کنید. تصاویر مفقود در فرایند ابزار به placeholder هدایت می‌شوند.
- تم: وضعیت `data-theme` در HTML و منطق toggle در JS باید هماهنگ بمانند.
- نسخه کش بررسی‌شده: `trendcargo-v5.1.0`؛ HTML به روش network-first و assets به روش cache-first. پس از تغییر دارایی‌های اجرایی نسخه کش را افزایش دهید؛ این تغییر صرفاً مستنداتی نیاز ندارد.
- CSP فعلی img-src برابر `'self' data: https://fonts.gstatic.com` است؛ خلاصه README/AGENTS درباره آن دقیقاً همه منابع مجاز را ذکر نمی‌کند.
- Vercel برای css/js/assets کش یک‌ساله immutable دارد؛ هنگام انتشار، کش مرورگر و سرویس‌ورکر را بررسی کنید. `sw.js` باید revalidate شود.

## اجرا و اعتبارسنجی
### سایت استاتیک (Vercel / فایل)
دستورها را از ریشه پروژه اجرا کنید:
```text
python -m http.server 8000
python pack_project.py
node tools/localize-images.cjs --dry-run
node tools/localize-images.cjs
```
سرور: http://localhost:8000 ؛ بسته: `trendcargo-final.zip`، بدون backend/tools/data.
تست خودکار کامل frontend پیکربندی نشده است. وجود ابزار smoke به معنای پاس‌شدن تست‌ها نیست. برای تغییر اجرایی، جریان مرتبط و کنسول مرورگر را بررسی کنید.

### سایت داینامیک با همگام‌سازی لحظه‌ای (Node + SSE)
وقتی بک‌اند اجرا باشد، ادمین روی سرور ذخیره می‌کند و همهٔ دستگاه‌ها بلافاصله به‌روز می‌شوند:
```text
cd backend
npm install
npm start            # http://localhost:3000 — هم سایت و هم API
```
- لود سایت: `GET /api/store` (در `RemoteSync` داخل `js/script.js`).
- اعلان تغییر: `GET /api/events` (EventSource) — هر `POST` موفق در `backend/routes/*` رویداد `store-update` می‌فرستد.
- بدون سرور: رفتار فعلی localStorage حفظ می‌شود؛ پیام‌های toast ادمین وضعیت انتشار را اعلام می‌کنند.
- استقرار پیشنهادی: یک سرویس Node (Render/Railway/VPS). `backend/` روی Vercel استاتیک اجرا نمی‌شود.

## گام‌های بعدی و نگهداری حافظه
1. `backend/data/news.json` فعلاً `[]` است تا تست دستی قبلی پاک بماند؛ اگر خبر واقعی خواستید از ادمین اضافه کنید.
2. پیش از کار جدید `git status --short` و `git --no-pager log -5 --oneline` را ببینید؛ تغییرات پس از مبنای این سند را هدفمند بررسی کنید.
3. همه کارها مستقیم روی `main` انجام و پوش شود؛ برنچ جدید نسازید مگر برای کارهای آزمایشی بزرگ. برنچ پشتیبان `backup/pre-unify-20260917` را حذف نکنید.
4. پس از هر کار مهم، همین سند را با نتیجه، فایل‌های تغییرکرده، آزمون‌های واقعاً اجراشده و موارد باقی‌مانده به‌روز کنید؛ گزارش‌های قدیمی را خلاصه نگه دارید.
5. استقرار زنده فعلاً استاتیک روی Vercel است (`/api/health` 404 می‌دهد). برای فعال‌شدن همگام‌سازی لحظه‌ای، طبق `docs/DEPLOY.md` بک‌اند را روی Render/Railway مستقر کنید و دامنه را به آن منتقل کنید.

## خروجی این جلسه
### اخبار و ادمین
- `index.html`: کانتینر دکمه «نمایش اخبار بیشتر» زیر گرید اخبار.
- `css/style.css`: اصلاح `overflow` دیالوگ مودال اخبار تا محتوا اسکرول شود + استایل دکمه بیشتر.
- `js/script.js`: نمایش پیش‌فرض ۳ خبر (`NEWS_DEFAULT_LIMIT`) با دکمه بیشتر؛ سازگاری `normalizeNewsItem` با شکل داده ادمین (`tag`→`category`/`badge`، پیش‌فرض زمان مطالعه).
- `admin.html`: کارت اخبار با «متن کامل» و «زمان مطالعه»؛ ذخیره هم‌شکل با فرانت؛ id پایدار از دیتا؛ حذف با مقایسه رشته‌ای id.
- `sw.js`: کش `trendcargo-v5.2.0`.
- اعتبارسنجی اخبار: `node --check js/script.js` و بررسی نحوی اسکریپت داخلی ادمین. تست مرورگری انجام نشده.

### سایت داینامیک + همگام‌سازی لحظه‌ای ادمین↔سایت (Node + SSE)
- `backend/routes/store.js` (جدید): `GET /api/store` تجمیعی + `GET /api/events` استریم SSE با heartbeat و شمارنده نسخه.
- `backend/server.js`: غیرفعال‌شدن compression برای `/api/events`؛ ثبت route جدید؛ `app.locals.broadcastStoreUpdate`.
- `backend/routes/{products,news,rates,special-offer,testimonials,discounts}.js`: اعلان `store-update` پس از هر ذخیره موفق.
- `js/script.js`: `RemoteSync` — لود از `/api/store`، رندر مجدد همه بخش‌ها، اشتراک SSE با debounce؛ بدون سرور = رفتار قبلی localStorage.
- `admin.html`: ذخیره محصولات/پیشنهاد/اخبار/نرخ/نظرات علاوه بر local روی `/api/*`؛ toast وضعیت انتشار (منتشر شد / سرور در دسترس نیست).
- `tools/sse-check.cjs`: اسکریپت کمکی تست دستی SSE.
- اعتبارسنجی backend: `node --check` همه فایل‌ها؛ تست واقعی با `DATA_DIR` موقت — هر ۶ فایل seed روی مسیر جدید کپی و `/api/store` سالم برگشت؛ پوشه تست پاک شد.
- نکته: احراز هویت ادمین از `ADMIN_USER` و `ADMIN_PASSWORD_SHA256` محیطی می‌آید (fallback توسعه حفظ شده).

### استقرار و ماندگاری داده (بخش DEPLOY)
- `backend/server.js` + هر ۷ route: `DATA_DIR` از `process.env.DATA_DIR`؛ seed خودکار فایل‌ها روی دیسک تازه در اولین بوت.
- `backend/Procfile` (`web: node server.js`)، `backend/.env.example` (+`DATA_DIR`)، `docs/DEPLOY.md` (+بخش Render Disk و اتصال دامنه شخصی با DNS).
- اعتبارسنجی: تست بالا (seed + store) پاس شد.

### بازنویسی ادمین + فرمول شفاف قیمت (بدون دلار)
- `admin.html`: حذف کامل نمایش دلار (متریک «قیمت دلار»→«استعلام‌های جدید»، فاکتور فقط تومانی بدون دراپ‌داون ارز، تنظیمات: «نرخ تبدیل ارز (تومان)» + «حمل اولین/هر کیلو اضافه»)؛ گوش‌دهندهٔ SSE ادمین (`subscribeAdminSync`) برای سینک دوطرفهٔ لحظه‌ای؛ بخش جدید «استعلام‌های مشتریان» با تغییر وضعیت (`new/contacted/invoiced/closed`)
- `backend/routes/orders.js` (جدید): `POST /api/order` عمومی + `GET /api/orders` + `POST /api/orders/status` با اعلان SSE؛ seed `orders.json` اضافه شد
- `index.html` + `css/style.css`: جعبهٔ «فرمول شفاف قیمت» در بخش ثبت لینک خرید (ارزش محصول + ۲۵٪ + ۱۲٪ + ۲.۵م بررسی + حمل ۳.۵م/۲.۸م هر کیلو — گرد به ۱۰ هزار) + فیلدهای اختیاری نام/تلفن مشتری
- `js/script.js`: `submitCustomLink` حالا استعلام را به `/api/order` ثبت می‌کند (بدون توقف جریان واتساپ) و شماره تماس را در پیام می‌گنجاند
- `invoice.html`: نمایش فقط تومانی (حذف USD/نرخ)
- `sw.js`: کش `trendcargo-v5.3.0`
- اعتبارسنجی: `node --check` همهٔ فایل‌ها + اسکریپت داخلی ادمین؛ تست واقعی سرور: POST/GET order و تغییر وضعیت همه پاس شدند؛ کلیدهای داخلی `baseRates.usd` برای سازگاری داده حفظ شدند (فقط نمایش دلار حذف شد)
